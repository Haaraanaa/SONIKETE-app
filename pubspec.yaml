name: sonikete
description: App cofrade con más de 180 formaciones musicales
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/data/formaciones.json
